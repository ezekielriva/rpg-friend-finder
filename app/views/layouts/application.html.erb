<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no' name='viewport'>
  <title>RPG Friend Finder Proto</title>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= csrf_meta_tags %>
  <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
        <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>
<body class="skin-purple layout-boxed sidebar-mini sidebar-collapse">
  <%= javascript_include_tag "facebook_integration.js" %>
  <div class="wrapper">
    <%= render "shared/main_header" %>
    <%= render "shared/main_sidebar" %>
    <div class="content-wrapper">
      <!-- Content Header (Page header) -->
      <section class="content-header">
        <h1>
          <%= content_for?(:title) ? yield(:title) : @title %>
          <small><%= content_for?(:subtitle) ? yield(:subtitle) : @subtitle %></small>
        </h1>
        <ol class="breadcrumb">
          <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
          <li><a href="#">Examples</a></li>
          <li class="active">Blank page</li>
        </ol>
      </section>

      <!-- Main content -->
      <section class="content">
        <% if notice %>
          <div class="alert alert-info alert-dismissable">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
            <%= notice %></div>
        <% end %>
        <% if alert %>
          <div class="alert alert-danger alert-dismissable">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
            <%= alert %></div>
        <% end %>
        <%= yield %>
      </section><!-- /.content -->
    </div>
    <%= render "shared/main_footer" %>
    <%= render "shared/control_sidebar" %>
  </div>
  <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBFAxUiVg13Fx07MTBKto0Qk0u9PS6vKWE&v=3.exp' +
        '&signed_in=false&libraries=places"></script>
  <%= javascript_include_tag 'application' %>
</body>
</html>
