<div class="row">
  <div class="col-sm-3"></div>
  <div class="col-sm-9">
    <div class="box box-default">
      <div class="box-header with-border">
        <i class="fa fa-users"></i>
        <h3 class="box-title">Games</h3>
        <%= link_to new_game_path, class: "btn btn-xs btn-primary btn-flat pull-right" do %>
          <i class="fa fa-plus"></i>
          New
        <% end %>
      </div>
      <table class="table">
        <thead>
          <tr>
            <th>Name</th>
            <th></th>
            <th></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @games.decorate.each do |game| %>
            <tr>
              <td><%= game.name %></td>
              <td><%= link_to show_link_label, game %></td>
              <td><%= link_to edit_link_label, [:edit, game] %></td>
              <td><%= link_to delete_link_label, game, method: :delete, data: {confirm: "Are you sure?"} %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
