<table class="table">
  <thead>
    <tr></tr>
  </thead>
  <tbody>
    <% members.each do |member| %>
      <tr>
        <td><%= image_tag member.avatar, class: "img-circle" %></td>
        <td><%= member %></td>
        <td><%= link_to delete_link_label, [@group, member], method: :delete, data: {confirm: "Are you sure?"} %></td>
      </tr>
    <% end %>
  </tbody>
</table>
